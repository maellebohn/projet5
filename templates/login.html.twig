{% extends 'base.html.twig' %}

{% block wrapper %}
    <div class="row title_row">
        <div class="col-xs-12 col-md-offset-3 col-md-6 title">
            <h1>CONNEXION</h1>
        </div>
    </div>

    {% for message in app.flashes('success') %}
        <div class="flash-success">
            {{ message }}
        </div>
    {% endfor %}

    {% form_theme form 'theme/login_type.html.twig' %}
    {{ form(form) }}

    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <p><a href="{{ path('ask_reset_password') }}">Mot de passe oublié ?</a></p>
        </div>
    </div>

{% endblock %}
